coverage:
  status:
    project:
      default: off
      react-button:
        flags:
          - react-button

flags:
  react-button:
    paths:
      - packages/pongo-ui/packages/react-button
    carryforward: true

comment:
  layout: 'reach,diff,flags,files,footer'
  behavior: default
  require_changes: no
